name: trade-vault-backend
description: Elite Trading App Backend with MongoDB
runtime: node

build:
  cmd: npm install && npx prisma generate && npx prisma db push

env:
  - key: DATABASE_URL
    value: mongodb+srv://sajjadriaz51_db_user:jjQUydJ0nwfBKDTY@cluster0.t4e5uog.mongodb.net/TradeVault?retryWrites=true&w=majority
    description: MongoDB Connection String
  
  - key: JWT_SECRET
    value: trade_vault_secret_786
    description: JWT Secret Key
  
  - key: NODE_ENV
    value: production
    description: Environment Mode
  
  - key: PORT
    value: '5000'
    description: Server Port

startCommand: npm start
